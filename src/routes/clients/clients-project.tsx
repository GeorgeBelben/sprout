import { ReactElement } from "react";
import { Page, PageHeader } from "~/components";

export function ClientsProjectRoute(): ReactElement {
	return (
		<Page>
			<PageHeader title={"Project name"} />
		</Page>
	);
}
